//create a new array by appliying a function to each element
//transform the array without changing the original array areturns a new arraray transformed values

const double=[1,2,3].map(num=>num*2);
console.log(double);
